---
import Layout from "../../layouts/Layout.astro";
import PlaylistItemCard from "../../components/PlaylistItemCard.astro";
import { allPlaylists, songs } from "../../lib/data";

const { id } = Astro.params;

//Extraer de las playlist las mismas que tengan el id que viene por parametros
const playList = allPlaylists.find((playlist) => playlist.id === id);
const playListSongs = songs.filter(
  (song) => song.albumId === playList?.albumId
);
---

<Layout title="Spotify clon">
  <div
    id="playlist-container"
    class="relative transition-all duration-1000 bg-green-600"
  >
    <!-- Aca va el page header -->

    <div class="relative z-10 px-6 pt-10"></div>
    <div
      class="absolute inset-0 bg-gradient-to-t from-zinc-900 via-zinc-900/80"
    >
    </div>
  </div>
</Layout>

<style></style>
